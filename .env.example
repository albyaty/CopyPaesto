# Public WebSocket origin for the deployed relay, without a trailing slash.
# Local development automatically uses ws://localhost:8787.
VITE_RELAY_URL=wss://copypaesto-relay.YOUR_SUBDOMAIN.workers.dev
